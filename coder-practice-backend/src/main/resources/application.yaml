spring:
  application:
    name: coder-practice-backend
  datasource:
    driver-class-name: # <todo> ???
    # <todo> url ?????
    username: root
    password: 123456

server:
  port: 8001
  servlet:
    context-path: /api


mybatis-plus:
  configuration:
    # MyBatis ??
    map-underscore-to-camel-case: false
  global-config:
    db-config:
      # ????
      logic-delete-field: isDelete

# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      # ????????????
      packages-to-scan: com.codebear.coderpracticebackend.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
